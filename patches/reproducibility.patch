diff --git a/Dockerfile b/Dockerfile
index 30220f4..2f4db20 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -9,7 +9,7 @@
 # that only happens once.
 #
 #   docker compose --profile single up -d      (see docs/docker.md)
-FROM nvidia/cuda:13.0.1-base-ubuntu24.04
+FROM nvidia/cuda:13.0.1-base-ubuntu24.04@sha256:f8ef28f579ea42a44b415d2c5d46f788e6a9b395c6c83f2929416e1fc192c143
 
 ENV DEBIAN_FRONTEND=noninteractive PIP_NO_CACHE_DIR=1 PYTHONUNBUFFERED=1
 RUN apt-get update && apt-get install -y --no-install-recommends \
diff --git a/docker/prepare.sh b/docker/prepare.sh
index ae0a8c8..f46d895 100755
--- a/docker/prepare.sh
+++ b/docker/prepare.sh
@@ -11,6 +11,7 @@ cd /app
 export PATH=/app/venv/bin:$PATH
 BASE=${BASE_MODEL_DIR:-/app/models/Qwen3.8-27B-W4A16-AutoRound}
 HF_REPO=${HF_REPO:-dbirks/Qwen3.8-27B-W4A16-AutoRound}
+HF_REVISION=${HF_REVISION:-1f05c441c4e64ae0549de44fa9ea5a6d43610314}
 
 state() {  # prints the steps still to do
 python - "$BASE" <<'EOF'
@@ -41,7 +42,7 @@ EOF
 TODO=$(state)
 if [ "$TODO" = "download" ]; then
   echo "== downloading $HF_REPO -> $BASE (~19.5 GB, resumable)"
-  hf download "$HF_REPO" --local-dir "$BASE"
+  hf download "$HF_REPO" --revision "$HF_REVISION" --local-dir "$BASE"
   TODO=$(state)
 fi
 [ "$TODO" = "download" ] && { echo "prepare: download incomplete (shards missing after hf download)"; exit 1; }
diff --git a/prepare/fetch_dflash2.py b/prepare/fetch_dflash2.py
index 9aecf48..33d06d6 100644
--- a/prepare/fetch_dflash2.py
+++ b/prepare/fetch_dflash2.py
@@ -13,8 +13,10 @@ HERE = os.path.dirname(os.path.abspath(__file__)); ROOT = os.path.dirname(HERE)
 BF16 = "--bf16" in sys.argv
 args = [a for a in sys.argv[1:] if not a.startswith("--")]
 REPO = "incoai/Qwen3.8-27B-DFlash2" if BF16 else "syvai/Qwen3.8-27B-DFlash2-W4A16"
+REVISION = None if BF16 else "4d30ec736ffc6b8688dc2ae2b502d9b48bdec279"
 D = args[0] if args else os.path.join(ROOT, "models", "Qwen3.8-27B-DFlash2" + ("" if BF16 else "-W4A16"))
 os.makedirs(D, exist_ok=True)
-snapshot_download(REPO, local_dir=D, allow_patterns=["*.json", "*.safetensors", "README.md"])
+snapshot_download(REPO, revision=REVISION, local_dir=D,
+                  allow_patterns=["*.json", "*.safetensors", "README.md"])
 print("drafter ready:", D)
 print("serve with: SPEC=dflash2 bash single-user/start_qwen.sh")
diff --git a/prepare/fetch_fast_variant.py b/prepare/fetch_fast_variant.py
index 27dbe3f..aad5445 100644
--- a/prepare/fetch_fast_variant.py
+++ b/prepare/fetch_fast_variant.py
@@ -32,6 +32,7 @@ assert os.path.exists(os.path.join(D, "tokenizer.json")), (
     f"no tokenizer.json in {S} — the base model dir is incomplete; re-run the download "
     f"(README: Setup) before building the fast variant")
 hub = snapshot_download("syvai/qwen3.8-27b-3090-fast-variant",
+                        revision="124c14e7e8c7d2f5402933b9af368e772a9fcf0c",
                         allow_patterns=["model-00007-of-00007.safetensors", "model_extra_tensors.safetensors",
                                         "mtp_draft_vocab_ids.pt", "config.json", "model.safetensors.index.json"])
 for f in ["model-00007-of-00007.safetensors", "model_extra_tensors.safetensors", "mtp_draft_vocab_ids.pt",

